# FIW 亲子验证实验说明

**当前最好结果：官方 Test 四类平均 AUC = 0.8394**  
实验名称：`112-adaface-mlp-fusion`  
日期：2026-08-17  
任务：FIW Track I，亲子四类关系 FD / FS / MD / MS 的二分类验证  

本文先给出当前最好结果，再按时间线说明数据协议与模型上的改动、各轮实验的有效与失败原因、0.8394 的构成，以及后续可尝试的方向。评测分表见同目录 [`评测结果.md`](评测结果.md)。11 类见 `fiw-11rel/`。

**本目录定位**：本目录为 `fiw-difficult/`——难样本加权实验的正式工作目录。代码、日志与权重完整继承自已验证的融合基线 `fiw-fusion/`（2026-08-20 状态，两者代码逐字节一致，基线权重 md5 相同）。当前尚未包含任何难样本加权实现（`config.py` 中 `USE_WEIGHT_DFC = False`），后续难样本实验从这里开始。

---

## 1. 主要结论

最好结果**不是**单独把 DualFDM / 模糊模块训到最强，而是两条路融合：

1. **冻住的 AdaFace IR-101**（WebFace4M 人脸识别预训练）：两张脸 512 维特征做余弦。这一路**没有**在 FIW 上微调主干，也**没有**接 DualFDM。单路 Test AUC **0.7782**。
2. **我们自己在 FIW 上训练的 112-MLP**（Swin-Tiny ImageNet 预训练 + DualFDM + 加宽分类头）：用 DualFDM 得到的亲缘特征 `F_kin` 再做余弦。单路 Test AUC **0.7539**。
3. 两路分数在**验证集**上做 z-score 后加权，AdaFace 权重 **α = 0.60**。官方测试集只评一次，得到 **0.8394**。

AdaFace 这一路贡献更大；112-MLP 则形成互补，尤其补了 AdaFace 较弱的父女（FD）。融合相对单路冻余弦大约高 **0.061**。

若将 DualFDM 理解为“使识别特征更适于亲缘判别”，实验**不支持**这一解释：把 DualFDM 接到 AdaFace 的 1×1 身份向量上，测试从 0.778 掉到 0.741。当前指标成立，但方法贡献的表述需要与该事实一致。

---

## 2. 任务、数据与评测口径

### 2.1 任务

给定父母、子女各一张已裁好的人脸，判断是否为亲子。四类关系分开报 AUC，再取平均：

| 简称 | 含义 |
| --- | --- |
| FD | Father–Daughter 父女 |
| FS | Father–Son 父子 |
| MD | Mother–Daughter 母女 |
| MS | Mother–Son 母子 |

主指标与评测表中的 **Mean** 相同：**四类关系 AUC 的算术平均**。同时报 ACC@0.5、ACC@best（Youden 最优阈值）、EER，以及高斯噪声鲁棒 AUC。

### 2.2 数据从哪来、划分怎么定

图像：`data/FIW/FIDs/` 下按家庭编号存放。  
配对名单：`data/FIW/pairs/*.txt`，每行五列：

```text
<FID> <父母相对路径> <子女相对路径> <FD|FS|MD|MS> <label>
```

`label=1` 正样本，`label=0` 负样本。

| 划分 | 规模 | 说明 |
| --- | --- | --- |
| 训练集 | 151951 正 / 151951 负，549 个家庭 | 正负 1:1；负样本跨家庭；抽样种子 42 |
| 验证集 | 28134 正 / 28134 负 | 同样 1:1、跨家庭；**只用来选模型 / 选融合系数** |
| 测试集 | 17231 对（9354 正 / 7877 负） | **FIW 官方 Track I 名单，一对都没有改** |

图片缺失：完整数据上 train/val/test 缺失为 0。中间有一次换机器缺了约 1.2 万训练对（506 家），val/test 数量未变；112-MLP 最终那轮数据已齐。

### 2.3 相对初始代码，数据协议改了什么

初始代码中，配对文件主要是正样本，负样本在训练时于 **batch 内现场生成**（例如把同一个 batch 里的子女互换）。这样会有问题：

- 同一 batch 里若出现多对来自同一家庭的样本，换出来的“负样本”仍可能是一家人，模型却会把他们往远处推（亲缘 BCE 和 DFC 对比损失都会受影响）。
- 测试若也现场造负，就和官方 Track I 对不上，数字无法和文献比。

因此这一轮做了三件规范的事：

1. **测试集完全使用 FIW 官方 `test.txt`**，正负都用官方标注，不再现场造负。
2. **训练集、验证集事先写好固定负样本文件** `train-neg.txt` / `val-neg.txt`：跨家庭配对，正负 1:1，训练时直接读名单。
3. DFC 对比损失里再加一层屏蔽：同一家庭、同一人不能当负对。

验证/测试名单始终走官方划分，家庭限权只改变**训练抽样频率**，不改 txt 内容。

---

## 3. 模型与训练设置的变更

下面按模块说。原始出发点是 GiFNet 风格的多粒度模糊亲缘网络：主干提特征 → DualFDM 把性别/年龄与亲缘分开 → FIM 融合父母子女 → 分类头 + DFC 对比损失。

### 3.1 主干与输入

| 阶段 | 主干 | 输入 | 预训练 |
| --- | --- | --- | --- |
| 最初 | Swin-Tiny，无预训练 | 64×64，RGB+HSV+Lab 九通道 | 无 |
| 112-MLP（融合里的第二路） | Swin-Tiny | 112×112，九通道 | ImageNet |
| AdaFace（融合里的第一路） | IR-101 | 112×112，BGR，归一化到 [-1,1] | WebFace4M（`adaface_ir101_webface4m.ckpt`） |

112-MLP 在冻结主干的前 3 轮里，九通道到三通道的 `conv_in` **仍训练**（随机初始化，必须动）。解冻后主干 lr = 2e-5，头 lr = 1e-4。

### 3.2 DualFDM / FIM / 头

- DualFDM：公共模糊分支 + 性别 mask +（可选）年龄 mask。亲缘特征 `F_kin` 对性别 mask **停梯度**，避免与性别监督争夺特征。年龄分支全程关闭（`USE_AGE_MASK=False`），不进前向、不进损失、不进优化器。
- FIM：父母/子女 `F_kin` 做绝对差、平方差、点积，再经 MT-FMF 融合成向量。
- 分类头：最终采用两层 MLP，`Linear(768→256)→ReLU→Linear(256→1)`，亲缘头与性别头都加宽。这就是实验名 **`112-imagenet-mlp-head`** 的由来。
- **评测/融合时的亲缘分数不是分类头 logit**，而是 `F_kin` 的余弦。分类头参与训练（BCE），测试打分看特征。

AdaFace 融合这一路：**整网 IR-101 都在前向，但完全冻结**；不用 DualFDM，不用亲缘头。

### 3.3 损失

正样本走多任务：亲缘 BCE（权重 0.25）+ 性别 BCE（0.10）+ DFC 余弦对比（其余 0.65）。负样本**只做亲缘 BCE**，避免 DFC 把非亲人当成正对来拉近。

后来关掉欧氏 DFC（`B_DFC=0`），因为欧氏项会在解冻后导致训练不稳定。难样本加权也关掉：原实现是整批乘一个平均权重，并没有按样本加权。

### 3.4 训练稳定性

解冻后一度出现 Val AUC 变成 0.50、损失变 0。排查结论：

- 关 AMP 后训练仍会崩溃，**不是 AMP 单独造成的**。
- DFC 里对特征先 L2 再算欧氏，崩溃更早。
- 关掉欧氏 DFC 后能解冻。
- 损失看起来有限、但梯度是 Inf 时跳过更新，避免污染 Adam（保护措施，并非根因修复）。
- **真正让测试从约 0.72 跳到约 0.75 的，是按家庭限权抽样**，而不是继续调节温度或学习率。

家庭限权：样本权重 = `1 / n_fid^power`。`power=1` 各家总次数相同；最终采用 **`power=0.5`（平方根）**。最大家 F0601 从训练次数的 38.6% 压到期望约 3.7%。验证/测试名单不变。

两阶段：前 3 轮冻主干，之后 CosineAnnealingLR，验证 AUC 连续 5 轮不涨早停。始终用 `best.pth`，不用 `last.pth`。

### 3.5 融合（当前最好，无再训练）

对每一对人脸，同一张图分别送给两个模型（噪声也只加一次）：

- AdaFace：BGR → IR-101 → 余弦 \(s_{\mathrm{Ada}}\)
- 112-MLP：RGB 九通道 → Swin+DualFDM → `F_kin` 余弦 \(s_{\mathrm{MLP}}\)

验证集上分别估计均值和标准差，做 z-score（测试集沿用，不再重算）。网格搜索 α∈[0,1]，步长 0.05：

\[
s = \alpha\, z_{\mathrm{Ada}} + (1-\alpha)\, z_{\mathrm{MLP}}
\]

验证 AUC 最高为 **α = 0.60**。另外用验证集训逻辑回归拼两路，测试只差约 0.001，主结果报加权。

**官方测试集不调 α、不重估均值方差。**

---

## 4. 当前最好系统：0.8394 的完整数字

实验名：`112-adaface-mlp-fusion`  
日志：本目录 `logs/112-adaface-mlp-fusion/eval_fusion_20260817_114350.log`（干净测试主结果）。性别 AUC 0.9481 为 2026-08-19 补测，干净测试数字未变。（补测日志 `eval_fusion_20260819_154911.log` 未随目录保留；当前目录 `eval_fusion.py` 为 2026-08-17 版、不含性别与整体混合输出，带该输出的版本见 `archive/fiw-fusion/eval_fusion.py`。）  
权重：`adaface_ir101_webface4m.ckpt` + `weights/112-imagenet-mlp-head/best.pth`  
命令：`python eval_fusion.py`

### 4.1 验证集（只用来选融合系数）

四类平均 AUC：

| AdaFace 余弦 | 112-MLP | 加权融合 α=0.60 | 逻辑回归 |
| --- | --- | --- | --- |
| 0.7690 | 0.6995 | **0.8104** | 0.8104 |

验证上加权和逻辑回归并列。测试只差 0.001，主结果报告加权融合。

### 4.2 官方 Test 标准 · 加权融合 α=0.60（主结果）

测试集 17231 对（正 9354 / 负 7877）。亲缘分数是两路融合。**Mean 为四类算术平均**，这是主指标。

| | F-D | F-S | M-D | M-S | **Mean** |
| --- | --- | --- | --- | --- | --- |
| AUC | 0.8071 | 0.8850 | 0.8467 | 0.8188 | **0.8394** |
| ACC@0.5 | 0.7292 | 0.8001 | 0.7656 | 0.7404 | **0.7588** |
| ACC@best | 0.7328 | 0.8026 | 0.7565 | 0.7416 | **0.7584** |
| EER | 0.2670 | 0.1985 | 0.2342 | 0.2590 | **0.2397** |

全部 17231 对混合计算一次（与四类平均不同口径）：AUC **0.8420**，ACC@0.5 **0.7608**，ACC@best **0.7616**，EER 0.2383。

性别只来自 112-MLP 的父母性别头，AdaFace 余弦没有性别头。与原表 Gender 列相同的口径是**仅正样本、阈值 0.5**：

| 口径 | n | 性别 ACC@0.5 | 性别 AUC |
| --- | --- | --- | --- |
| 仅正样本（与原 Gender 列一致） | 9354 | **0.8806** | **0.9481** |
| 全部测试对 | 17231 | 0.8765 | 0.9466 |

不能按 FD/FS/MD/MS 分别报性别 AUC：每一类父母性别是单一的。逻辑回归融合 Test 四类平均 AUC **0.8385**，不作为主结果。

### 4.3 官方 Test 噪声（同一套 α=0.60）

在图像上加 N(0,10) 的像素噪声，两路看到同一份噪声图。

| | F-D | F-S | M-D | M-S | **Mean** |
| --- | --- | --- | --- | --- | --- |
| AUC | 0.7683 | 0.8325 | 0.8121 | 0.7799 | **0.7982** |
| ACC@0.5 | 0.6969 | 0.7497 | 0.7289 | 0.7119 | **0.7219** |
| ACC@best | 0.7019 | 0.7548 | 0.7295 | 0.7148 | **0.7252** |
| EER | 0.3031 | 0.2498 | 0.2696 | 0.2888 | **0.2778** |

相对干净测试约掉 0.041。逻辑回归噪声 AUC 0.7984，几乎一样。

2026-08-19 为补性别 AUC 又跑了一次融合：干净测试与上表完全一致。噪声每次重新采样，该次加权融合四类平均 AUC 为 0.8000；噪声上性别（正样本）ACC@0.5 **0.8377**、AUC **0.9148**。主结果噪声仍报第一次的 **0.7982**。

### 4.4 同一次评测里的两条单路（对照）

| | Test AUC | ACC@0.5 | ACC@best | EER | 噪声 AUC | 性别 ACC@0.5 | 性别 AUC |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 冻 AdaFace 余弦 | 0.7782 | 0.6748 | 0.7128 | 0.2888 | 0.7656 | — | — |
| 112-MLP | 0.7539 | 0.5665 | 0.6929 | 0.3199 | 0.6730 | **0.8806** | **0.9481** |
| **加权融合** | **0.8394** | **0.7588** | **0.7584** | **0.2397** | **0.7982** | **0.8806** | **0.9481** |

分关系 AUC 对照：

| | FD | FS | MD | MS | Mean |
| --- | --- | --- | --- | --- | --- |
| 冻余弦 | 0.7298 | 0.8124 | 0.8018 | 0.7689 | 0.7782 |
| 112-MLP | 0.7358 | 0.8076 | 0.7433 | 0.7290 | 0.7539 |
| 融合 α=0.60 | **0.8071** | **0.8850** | **0.8467** | **0.8188** | **0.8394** |

AdaFace 在 FD 上弱于 MLP（0.730 vs 0.736），融完 FD 到 0.807，说明两路错法不同，不是简单重复。ACC@0.5 与 ACC@best 几乎相等（约 0.759），融合后分数更贴 0.5 阈值。

### 4.5 推理时用了整网还是只用头

两条**完整前向**都在用：

- AdaFace：整网 IR-101，冻住，余弦；无 DualFDM、无亲缘头、无性别头。
- 112-MLP：Swin + DualFDM + FIM + 分类头都走前向；融合用的亲缘分数是 `F_kin` 余弦，不是分类头 logit。性别分数取自同一套前向里的父母性别头 `head_gr`：正样本 ACC@0.5 = 0.8806，AUC = 0.9481（由 2026-08-19 版 `eval_fusion.py` 输出；当前目录中的 08-17 版无性别提取代码）。

---

## 5. 全部实验一览（到 0.8394 为止）

主指标均为官方 Test **四类平均 AUC**。未评测 test 的轮次标“—”。涨跌相对上一轮有对照意义的实验而言。

| # | 实验名 | 核心改动 | Val | Test | ACC@best | 噪声 | 结论 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 64-scratch-orig-code | 原始代码，batch 内造负 | — | — | — | — | 协议不可比，不与后面比 |
| 2 | 64-scratch-fiw-official | 官方划分 + 跨家庭负样本 txt | 0.6275 | **0.6699** | 0.6292 | — | 第一组能报的官方数字 |
| 3 | 112-imagenet-pretrain | 112 + ImageNet Swin | 0.6743 | **0.7163** | 0.6676 | 0.6532 | 大涨；解冻后易过拟合 |
| 4 | 112-imagenet-cosine | 解冻 cosine + 早停 | 0.6761 | 0.7165 | 0.6615 | 0.6557 | 持平，早停正常 |
| 5 | 112-imagenet-fp32 | 关 AMP | 0.6754 | — | — | — | 仍崩溃，不是 AMP |
| 6 | 112-imagenet-dfc-l2 | DFC 先 L2 | — | — | — | — | 更早崩溃，已改回 |
| 7 | 112-imagenet-cs-only | 关掉欧氏 DFC | 0.6789 | **0.7198** | 0.6699 | 0.6568 | 解冻正常，小涨 |
| 8 | 112-imagenet-lr-bb | 主干 lr 2e-5→1e-5 | 0.6646 | — | — | — | 更早崩溃 |
| 9 | 112-imagenet-finite-grad | Inf 梯度跳过更新 | 0.6835 | **0.7214** | 0.6723 | 0.6572 | 保护措施，略涨 |
| 10 | 112-imagenet-tau02 | 温度 0.08→0.2 | 0.6541 | — | — | — | 更差 |
| 11 | 112-imagenet-fam-bal | 按家庭均权抽样 | 0.7010 | **0.7472** | 0.6884 | 0.6848 | **明显提升** |
| 12 | 112-imagenet-fam-sqrt | 1/sqrt 家庭次数 | 0.6996 | **0.7520** | 0.6913 | 0.6791 | 再涨；F0601 期望 3.7% |
| 13 | 224-imagenet-fam-sqrt | 输入 224 | 0.7101 | 0.7521 | 0.6926 | 0.6553 | Test 持平，更慢更怕噪声 |
| 14 | 112-imagenet-head-lr | 解冻头 lr 3e-5 | 0.6963 | 0.7394 | 0.6811 | 0.6728 | 失败 |
| 15 | **112-imagenet-mlp-head** | 分类头加宽为两层 MLP | 0.6986 | **0.7539** | 0.6929 | 0.6736 | 单路 Swin 最好 |
| 16 | 112-imagenet-spatial-fdm | DualFDM 在特征图上解耦 | 0.6963 | 0.7520 | 0.6842 | 0.6827 | 略降 |
| 17 | 112-imagenet-mlp-ema | 解冻后 EMA | 0.7023 | 0.7518 | 0.6846 | 0.6923 | Val 涨 Test 不涨 |
| 18 | 64-celeba-mim-mlp | CelebA SimMIM，改回 64 | 0.6800 | 0.7355 | 0.6715 | 0.7196 | 好于 64 从零，不如 112 |
| 19 | 112-adaface-ir101-webface4m | AdaFace + DualFDM 再解冻 | 0.7015 | 0.7412 | 0.6774 | 0.7354 | **失败**，低于冻余弦 |
| 20 | 112-adaface-ir101-cosine | 冻 AdaFace，只算余弦 | — | **0.7782** | 0.7129 | 0.7672 | 单路最好；无训练 |
| 21 | **112-adaface-mlp-fusion** | 冻余弦 × 112-MLP | 0.8104 | **0.8394** | 0.7584 | 0.7982 | **当前最好** |
| 22 | 112-adaface-ir101-probe | 冻 AdaFace + 浅亲缘头 | 0.7341 | 0.7475 | 0.6829 | 0.7349 | 单路失败 |
| 23 | 112-adaface-probe-mlp-fusion | 浅头 × 112-MLP | 0.7867 | 0.8211 | 0.7419 | 0.7727 | 融合也低于 0.8394 |

读表时的几条线：

- **协议规范后**：无法报数 → 0.67。
- **112 + ImageNet**：0.67 → 0.72。
- **家庭限权**：0.72 → 0.75。这是 DualFDM 路线上最大的一次提升。
- **分类头加宽**：0.752 → 0.754，只是微涨。
- **224 / EMA / 空间 DualFDM / CelebA-64 / 降头 lr**：都没超过 0.7539。
- **AdaFace 接 DualFDM**：0.754 → 0.741，失败。
- **冻 AdaFace 余弦**：无训练到 0.778。
- **与 112-MLP 融合**：0.778 → **0.839**。

---

## 6. 关键实验细节

### 6.1 112-MLP（融合的第二路）

- 实验名：`112-imagenet-mlp-head`
- 日志：`logs/112-imagenet-mlp-head/`（训练 `train_fiw_20260816_162655.log`，测试 `eval_fiw_20260816_170645.log`）
- 设置：IMG=112，BATCH=128，AMP，冻 3 轮，解冻头 lr=1e-4、主干 2e-5，FAM_BAL_POWER=0.5，B_DFC=0，无 EMA
- Val 0.6986 @ epoch 4；Test 0.7539；性别 0.8806；噪声 0.6736
- 分关系：FD 0.7358 / FS 0.8076 / MD 0.7433 / MS 0.7290

复现：本目录 `python train.py` 会写到 `weights/`。基线权重是 `weights/112-imagenet-mlp-head/best.pth`。第一次训练会由 timm 下载 ImageNet Swin。⚠ 当前 `config.py` 的 `EXP_NAME` 仍为 `112-imagenet-mlp-head`，直接训练会覆盖基线权重，新实验请先改 `EXP_NAME`。

### 6.2 冻 AdaFace 余弦（融合的第一路）

- 实验名：`112-adaface-ir101-cosine`
- 日志：`logs/112-adaface-ir101-cosine/eval_cosine_20260817_113322.log`
- **无亲缘训练**。正余弦均值 0.0880，负余弦均值 0.0095，差 0.0785
- Test 0.7782；噪声 0.7672，几乎不掉
- 分关系：FD 0.7298 / FS 0.8124 / MD 0.8018 / MS 0.7689  
  MD/MS 明显高于 112-MLP；FD 略低于 MLP

### 6.3 AdaFace + DualFDM（失败，必须写进报告）

- 实验名：`112-adaface-ir101-webface4m`
- 把 DualFDM 接在 L2 归一化的 512 维 1×1 embedding 上，冻 3 轮再解冻
- Test **0.7412**，低于冻余弦 0.778，也低于 112-MLP 0.754
- 性别却到 0.9627：识别预训练已经把性别做满，再解耦帮不上亲缘主指标
- 结论：不宜再解冻 AdaFace 并接入 DualFDM

### 6.4 浅亲缘头（失败）

动机：冻余弦虽强，但没用亲缘标签。于是冻住 IR-101，用 `|fp−fc|` 与 `fp⊙fc`（1024 维）进浅 MLP，BCE 只训头。

- Val 0.7341 @ epoch 8，早停 epoch 13
- Test **0.7475**，四类全低于冻余弦 0.778
- 再与 112-MLP 融合：Test **0.8211**，仍低于余弦融合 0.8394

结论：在冻结的 IR-101 上，**直接余弦是更好的单路打分**；再用 BCE 浅头会降低排序质量。该浅头方案已不再采用。

---

## 7. 方法贡献与实验结果的对应关系

不宜将 0.839 直接归因于 DualFDM。与实验一致的贡献表述如下：

1. **协议**：官方测试集保持不变；训练/验证使用固定跨家庭负样本，避免 batch 内造负把同一家庭样本推远。
2. **训练侧发现**：家庭不平衡是 DualFDM 路线从 0.72 到 0.75 的主要原因；欧氏 DFC、调节温度、输入 224、EMA、空间 DualFDM 都没有再提升主指标。
3. **特征空间发现**：FIW 亲子信号大量已经包含在大规模识别预训练中。冻结 AdaFace 余弦 0.778，超过在 FIW 上训练的 DualFDM 网络 0.754；把 DualFDM 接到身份向量上会削弱这条信号。
4. **系统结果**：识别余弦与亲缘网络互补，在验证集上确定加权系数，测试得到 0.839。AdaFace 权重大，MLP 补齐 FD 等短板。

关于方法新颖性：分数融合本身并非新方法，0.839 是系统结果。核心模块 DualFDM 尚未证明强于冻结识别余弦。后续若继续做方法层面的工作，需要在**超过冻结余弦 0.778** 的前提下，建立真正亲缘特有的建模，而不是继续拆解识别空间。

---

## 8. 后期可能的改进方向

按优先级排序；已确认无效的设置不再重复尝试。

### 8.1 建议优先（不训练或很少训练）

1. **按关系分别选 α**  
   现在全局 α=0.60。融合已经显示 MLP 在补 FD，FD/FS/MD/MS 各学一个 α，只在验证集上选，测试评一次。成本低，还没做过。
2. **贡献表述与实验结论对齐**  
   当前数字已足以构成完整实验报告；贡献宜写成协议规范、特征空间分析与两路融合，而不把 DualFDM 写成 0.839 的直接原因。

### 8.2 可以做，但要设淘汰线

3. **更强的识别预训练（如 WebFace12M 的 AdaFace）**  
   仍然冻住只算余弦，再与现有 112-MLP 融。单路若不超过现在的 0.778，不必融。下载和磁盘都大。
4. **在冻结识别特征上做亲缘建模，但不再采用 BCE 浅头**  
   浅头已经失败。若再尝试，单路必须超过 0.778，否则停止。IR-101 保持冻结。
5. **三路融合**  
   冻余弦 + 112-MLP + 其他互补模型。浅头不宜作为第三路（已验证会降低融合结果）。

### 8.3 明确不建议再做

- 再解冻 AdaFace 接 DualFDM / FIM / DFC  
- 再调 TAU、欧氏 DFC、头 lr 3e-5、输入 224 作为主线  
- 把 64 的 CelebA 权重插到 112，或把 112-MLP 权重加载进 AdaFace  
- 再训一个同类 BCE 浅头去替代余弦  
- 在测试集上调 α 或调阈值后作为主结果报告

---

## 9. 复现说明

本目录是亲子四类代码、日志与文档，完整继承自已验证的融合基线 `fiw-fusion/`（2026-08-20 状态，代码逐字节一致）。其他分路互不共享数据与权重：

- `fiw-age/`：亲子四类 + 年龄解耦
- `fiw-11rel/`：FIW 11 类（说明与评测见该目录）
- `kinfacew/`：KinFaceW 五折

实验谱：本目录 `experiences.txt` / `experiences.html`。评测表：[`评测结果.md`](评测结果.md)。

**需要训练的只有 112-MLP。AdaFace 不在 FIW 上训练。**

```bash
python eval_fusion.py   # 复现 0.8394（无再训练）
python train.py         # 训练 112-MLP（先改 EXP_NAME，否则覆盖基线 best.pth）
```

新实验请改 `EXP_NAME`，权重写到本目录 `weights/`。

划分 txt 已包含：`train.txt` / `train-neg.txt` / `val.txt` / `val-neg.txt` / `test.txt`。人脸图像当前为占位（`data/FIW/FIDs/PUT_FIDs_HERE`），评测/训练前需放回真实图像。

单模型评测脚本（`eval_adaface_cosine.py`、独立 `eval_fiw` 评测、probe 脚本）不在本目录，历史版本见 `archive/old_code/`。

基线日志：

```text
logs/112-imagenet-mlp-head/train_fiw_20260816_162655.log
logs/112-imagenet-mlp-head/eval_fiw_20260816_170645.log
logs/112-adaface-ir101-cosine/eval_cosine_20260817_113322.log
logs/112-adaface-mlp-fusion/eval_fusion_20260817_114350.log   ← 0.8394
```

---

## 10. 附件：112-MLP 与冻余弦的分项（便于核对日志）

**112-MLP 官方 Test（`eval_fiw_20260816_170645.log`）**

| | F-D | F-S | M-D | M-S | Mean | Gender |
| --- | --- | --- | --- | --- | --- | --- |
| AUC | 0.7358 | 0.8076 | 0.7433 | 0.7290 | 0.7539 | 0.8806 |
| ACC@0.5 | 0.5547 | 0.5561 | 0.5838 | 0.5710 | 0.5664 | — |
| ACC@best | 0.6754 | 0.7382 | 0.6685 | 0.6894 | 0.6929 | — |
| EER | 0.3343 | 0.2725 | 0.3328 | 0.3376 | 0.3193 | — |

日志把性别 ACC@0.5 印在 AUC 行的 Gender 列；该列不是性别 AUC。2026-08-19 补测：正样本性别 AUC **0.9481**（ACC 仍为 0.8806）。噪声测试上原日志 Gender ACC 为 0.8360；补测因噪声重采样为 0.8377，性别 AUC 0.9148。

**冻 AdaFace 余弦官方 Test（`eval_cosine_20260817_113322.log`）**

| | F-D | F-S | M-D | M-S | Mean |
| --- | --- | --- | --- | --- | --- |
| AUC | 0.7298 | 0.8124 | 0.8018 | 0.7689 | 0.7782 |
| ACC@0.5 | 0.6470 | 0.6844 | 0.6906 | 0.6768 | 0.6747 |
| ACC@best | 0.6683 | 0.7435 | 0.7361 | 0.7039 | 0.7129 |
| EER | 0.3338 | 0.2614 | 0.2656 | 0.2964 | 0.2893 |

（融合日志里同一次前向的余弦 ACC 与上面差 0.0001 量级，是实现细节，以融合日志汇总为准：余弦 0.7782 / MLP 0.7539 / 融合 0.8394。）

---

本文只覆盖亲子四类。代码、数据与权重以本目录为准。11 类见 `fiw-11rel/说明文档.md`。历史实验在仓库 `archive/`。
