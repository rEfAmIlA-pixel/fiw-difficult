torch==2.8.0
torchvision==0.23.0
timm==1.0.28
opencv-python>=4.8.0
scikit-learn>=1.2.0
numpy>=1.24.0,<2

# GPU 服务器先装 CUDA 版 torch（PyPI 默认 wheel 可能不含所需 CUDA 运行时）：
# pip install torch==2.8.0 torchvision==0.23.0 --index-url https://download.pytorch.org/whl/cu126
# 再执行 pip install -r requirements.txt 补齐其余依赖（torch 已满足会自动跳过）
